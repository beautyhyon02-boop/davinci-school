# 성취기준 데이터 (2022 개정 교육과정)

## 추출 날짜

2026-09-19

## 원본 파일

`C:\Users\beaut\OneDrive\바탕 화면\서논술형\` 폴더의 2022 개정 교육과정 별책 PDF.

| 파일 | 과목 |
| --- | --- |
| `[별책5] 국어과 교육과정.pdf` | 국어 |
| `[별책7] 사회과 교육과정.pdf` | 사회 (역사/한국사 성취기준 포함 → `한국사.json`으로 분리) |
| `[별책9] 과학과 교육과정.pdf` | 과학 |
| `[별책14] 영어과 교육과정.pdf` | 영어 |
| `[별책2] 초등학교 교육과정.pdf`, `[별책3] 중학교 교육과정.pdf`, `[별책4] 고등학교 교육과정.pdf` | 수학 (별책8(수학과 단독 별책)이 없어, 학교급별 총괄 별책에서 코드 라벨이 수학과인 성취기준만 걸러냄) |

## 추출 방법

`scripts/extract_standards.py` (pdfplumber로 텍스트 레이어 추출 후 정규식 파싱)를
아래와 같이 실행했다.

```bash
S="C:/Users/beaut/OneDrive/바탕 화면/서논술형"
python scripts/extract_standards.py "$S/[별책5] 국어과 교육과정.pdf" 국어 data/standards/국어.json
python scripts/extract_standards.py "$S/[별책7] 사회과 교육과정.pdf" 사회 data/standards/사회_raw.json  # 아래 설명대로 사회/한국사 두 파일로 나눔
python scripts/extract_standards.py "$S/[별책9] 과학과 교육과정.pdf" 과학 data/standards/과학.json
python scripts/extract_standards.py "$S/[별책14] 영어과 교육과정.pdf" 영어 data/standards/영어.json
```

사회과 별책은 코드 안의 한글 라벨(`역`, `한사`, `한국사` 포함 여부)로
`subject_for_code()`가 성취기준을 사회/한국사로 재분류하며, 추출 스크립트가
그 결과를 그대로 JSON에 기록한다. `사회_raw.json`은 이후 `subject` 필드
기준으로 `사회.json`/`한국사.json` 두 파일로 나누고 삭제했다.

수학과는 단독 별책(별책8)이 없어서, `scripts/extract_math.py`가
`[별책2] 초등학교 교육과정.pdf`/`[별책3] 중학교 교육과정.pdf`/
`[별책4] 고등학교 교육과정.pdf`(학교급별로 모든 교과의 성취기준을 모아 둔
총괄 별책, 별책4는 22MB) 전체를 `extract_standards.parse_standards()`로
파싱한 뒤 코드 라벨(`수`, `공수`, `대수`, `확통`, `기하`, `경수`, `인수`,
`직수`, `수과`, `수문`, `미적Ⅰ`, `미적Ⅱ` 등)이 수학과인 행만 걸러
`data/standards/수학.json`으로 저장한다.

```bash
python scripts/extract_math.py
```

파싱한 각 별책의 페이지 전체 텍스트는 `.superpowers/raw/`(git 추적 제외)에
캐시해 두고 있어서, 재실행해도 22MB짜리 별책4를 다시 pdfplumber로 여는
데 걸리는 몇 분을 매번 기다리지 않는다.

## 과목별 건수

| 파일 | 전체 | 초 | 중 | 고 |
| --- | --- | --- | --- | --- |
| `국어.json` | 257 | 87 | 51 | 119 |
| `사회.json` | 307 | 47 | 74 | 186 |
| `한국사.json` | 46 | 0 | 20 | 26 |
| `세계사.json` | 59 | 0 | 20 | 39 |
| `과학.json` | 473 | 102 | 87 | 284 |
| `영어.json` | 237 | 40 | 21 | 176 |
| `수학.json` | 435 | 121 | 60 | 254 |
| **합계** | **1767** | 397 | 333 | 1037 |

2026-09-20 이전에는 `한국사.json` 한 파일(92건)이었으나, "역사 분리"
절(아래)에서 설명하는 재분류로 `한국사.json`(46)/`세계사.json`(46) 두
파일로 나뉘었고, 같은 날 최종 검토(I7)에서 `사회.json`에 남아 있던
고등학교 일반선택 「세계사」(`[12세사XX-XX]`, 13건)도 `세계사.json`으로
옮겨(`subject`만 `세계사`로 바꾸고 텍스트는 그대로) 사회 307 / 세계사
59가 되었다(전체 1767건은 그대로). 7개 파일을 합쳐 코드(`code`)
1767개가 모두 고유하다(중복 없음).

## domain 필드

`(1) 듣기⋅말하기`처럼 짧은 영역 소제목이 성취기준 코드 앞에 바로 나오는
구간(초·중학교, 고등학교 공통국어/공통영어/통합사회/통합과학 등)에서는
`domain`을 자동으로 채웠다. 다만:

- 고등학교 선택과목처럼 영역 소제목 없이 곧장 성취기준이 나오는 구간은
  `domain`을 빈 문자열로 둔다(과정이 바뀌었는데 새 영역 제목을 못 찾으면
  직전 과정의 domain을 이어붙이지 않고 비운다 — 잘못된 값을 남기는 것보다
  비워두는 쪽을 택했다).
- "(1) 교수·학습 방향", "(3) 평가 방법"처럼 실제 내용 영역이 아닌 행정
  소제목은 `domain` 후보에서 제외했다.
- 2주차에 필요하면 과목별 화이트리스트 방식으로 더 정교하게 보강할 수 있다.

## 알려진 문제 / 한계

- `domain`이 비어 있는 행이 있다(위 설명 참고). DB나 화면에서 domain이
  필요하면 2주차에 보강한다.
- pdfplumber의 텍스트 추출은 표(그림/도식 안의 텍스트)를 완전히 배제하지
  못할 수 있다. 성취기준 코드가 있는 줄만 파싱 대상으로 삼고, 표 형태로
  삽입된 참고 자료는 코드 형식(`[숫자+한글+숫자]`)이 아니므로 대부분
  자연스럽게 걸러진다. 다만 육안 검수에서 이상 항목을 발견하면
  `scripts/extract_standards.py`의 `STOP`/`NOISE` 패턴을 보강해야 한다.
- 육안 검수 중 발견해 고친 것들(관련 pytest 케이스도 추가함):
  - 페이지 경계에서 성취기준 문장이 줄바꿈되며 쪽 번호/러닝헤드가 끼어드는
    문제 → 페이지별이 아니라 전체 텍스트를 이어붙여 파싱하고, "OOO
    교육과정"류 러닝헤드와 낱개 대시(-)/불릿 기호만 있는 줄을 잡음으로
    처리하도록 보강했다.
  - 과학과는 성취기준 뒤에 "<탐구 활동>" 같은 꺾쇠 소제목과 예시 활동을
    바로 붙여두는데, 이것이 성취기준 문장에 섞여 들어가는 문제 →
    "<...>" 형태의 줄도 성취기준 해설과 같은 종결 신호로 처리했다.
  - 고등학교 선택과목처럼 영역 소제목 없이 새 과목으로 넘어가는 경우,
    직전 과목의 domain이 잘못 이어붙는 문제 → 코드의 과목 라벨이 바뀌면
    새 영역 제목을 만나기 전까지 domain을 비우도록 했다.
- 수학과(별책8)는 폴더에 파일이 없어, 학교급별 총괄 별책(별책2/3/4)에서
  수학 코드만 걸러 추출한다(위 "추출 방법" 참고). 총괄 별책은 과목별
  별책과 레이아웃이 달라 아래와 같은 문제가 있었고, 모두
  `scripts/extract_standards.py`/`scripts/extract_math.py`에서 고치고
  `tests/extract_standards_test.py`·`tests/extract_math_test.py`에 실제
  관측 문자열 그대로 회귀 테스트를 추가했다:
  - 성취기준 바로 뒤에 다음 소단원명이 "글머리 기호(U+F000) + 소단원명"
    형태로 붙어 나옴(예: "구할 수 있다.  도수분포표와 상대도수") →
    U+F000으로 시작하는 줄은 소제목으로 보고 누적을 멈추도록 STOP 패턴에
    추가.
  - 페이지가 바뀌는 자리에 "쪽 번호" 다음 줄에 과목명만 있는 러닝헤드가
    따로 나옴(예: "234" 다음 줄에 "수학") → 현재 과목명과 완전히 같은
    한 줄은 잡음으로 처리.
  - 고등학교 수학(대수/미적분/경제 수학 등)은 "미적분Ⅰ/Ⅱ"처럼 코드
    라벨에 로마 숫자(Ⅰ·Ⅱ)를 쓰는데, 코드 정규식이 한글만 허용해 코드
    자체를 인식하지 못해 43개 성취기준이 통째로 사라져 있었다 → 라벨
    문자 클래스에 Ⅰ·Ⅱ 추가.
  - 고등학교 수학 성취기준 문장 안 수식 변수·기호(x, y, =, +, a, b, c,
    d, √, 2, n)가 폰트 문제로 유니코드 사용자 영역(PUA) 문자로 추출됨 →
    "[별책4] 고등학교 교육과정.pdf" 실제 페이지(471/473/506/576쪽)를
    pdfplumber로 렌더링해 눈으로 대조 확인한 값으로 치환하는 표
    (`extract_standards.MATH_PUA_GLYPHS`)를 추가했다.
  - 분수식·근호식처럼 2차원으로 쌓인 수식은 pdfplumber의 읽기 순서가
    뒤엉켜, 분자 부분이 코드 표시보다 앞선 줄에 나타나 바로 앞 성취기준
    (이미 "~다."로 끝난 문장) 뒤에 잘못 붙는다. 이미 끝난 문장 뒤에
    한글 없이 기호만 남은 꼬리는 잘라내도록 했고(`_TRAILING_SYMBOL_JUNK`),
    `[10공수2-03-04]`(유리함수)·`[10공수2-03-05]`(무리함수) 두 건은
    잘려나간 분자 정보를 정규식만으로 복원할 수 없어 473쪽을 직접 렌더링해
    확인한 원문 그대로 `scripts/extract_math.py`의
    `FORMULA_FIXUPS`에서 명시적으로 보정했다(추측이 아니라 원문 이미지
    대조 결과 — `.superpowers/pages/`에 렌더링한 페이지 이미지를 보관).
  - 위 수정 후에도 U+E06D(분수선/근호 위 가로줄) 자체는 문맥에 따라
    의미가 달라지는 순수 도형 요소라 특정 문자로 옮기지 않고 위 두 건의
    보정문에서 형태소 그대로("(ax+b)/(cx+d)", "√(ax+b)+c") 반영했다.
- 무작위 10행(초 3·중 2·고 5)을 pdfplumber로 해당 페이지를 직접 렌더링해
  대조한 결과 모두 정확히 일치했다(코드/문장/영역 모두 일치, 2026-09-19).

## DB 투입 이력

- 2026-09-19: 마이그레이션 적용 후
  `npx dotenv -e .env.local -- npx tsx scripts/import-standards.ts` 실행 →
  호스팅 DB `standards` 테이블에 1379행 upsert 완료(`code` 기준).
- JSON을 다시 추출·수정한 뒤에는 같은 명령을 다시 실행하면 된다(`code`가
  같은 행은 덮어쓴다).
- 2026-09-20: 역사 분리(한국사 46 / 세계사 59, `[12세사]` 13건 포함)는
  파일에만 반영된 상태. 마이그레이션 0006(세계사 enum) 적용 후
  `scripts/reclassify-history.ts`(update-db 모드)를 한 번 실행하면 DB의
  59행이 `세계사`로 바뀐다(아래 "스크립트 사용법").


## 수학 정본·교차검증 (2026-09-19)

- 정본: `[별책8] 수학과 교육과정.pdf` (소유자가 별책8 .hwp를 한글에서 PDF로 저장) → `scripts/extract_standards.py … 수학` 로 추출, 435개(초 121 / 중 60 / 고 254).
- 교차검증: 학교급 총괄 별책(2·3·4)에서 `scripts/extract_math.py`로 독립 추출한 388개와 코드별 대조 → 공통 388개 중 **386개 글자 단위 동일**, 2개(공수2-03-04/05)는 분수·근호 표기 차이뿐. 별책8에만 있는 47개는 기본수학(`기수`) 34개·실용통계(`실통`) 13개로 총괄 별책에 수록되지 않은 과목.
- 수식 4건(`[10공수2-03-04]`, `[10공수2-03-05]`, `[10기수2-03-04]`, `[10기수2-03-05]`)은 pdfplumber가 분수·근호를 흐트러뜨려, 별책8 PDF 73·92쪽을 이미지로 렌더링해 눈으로 확인한 뒤 선형 표기(`(ax+b)/(cx+d)`, `√(ax+b)+c`, `k/x`, `√(kx)`)로 기록했다. 이 4건 외에 수기 수정은 없다.
- DB 투입: 2026-09-19, standards 총 1,814행(수학 435 포함).

## 역사 분리 (2026-09-20)

`scripts/extract_standards.py`의 `subject_for_code()`는 사회과 별책 코드의
라벨에 `한국사|한사|역`이 포함되면 통째로 `한국사`로 재분류한다. 이 정규식이
`역`을 포함하는 라벨을 전부 잡아 버려서, 실제로는 세계사 성격인 중학교
「역사」앞부분(`9역01`~`9역07`)과 고등학교 진로/융합선택 「동아시아 역사
기행」(`12동역`)·「역사로 탐구하는 현대 세계」(`12역현`)까지 모두
`한국사.json`(92건)에 섞여 있었다. (고등학교 일반선택 「세계사」는 코드
라벨이 `세사`라서 애초에 이 정규식에 걸리지 않고 `사회.json`에 남아
있었는데, 2026-09-20 최종 검토(I7)에서 그 13건도 `세계사.json`으로 옮겼다
— 아래 "결과 건수" 참고.)

`scripts/reclassify-history.ts`의 `classifyHistory(code, domainOrText)`가
아래 규칙에 따라 이 92건을 다시 `한국사`/`세계사`로 나눈다. **결정은 추측이
아니라 `[별책7] 사회과 교육과정.pdf`(물리 79쪽, 인쇄쪽수 73, 러닝헤드
"공통 교육과정")의 다음 문단과 실제 성취기준 문장 대조로 확정했다**:

> 역사과의 공통 교육과정 영역은 한국과 세계의 역사를 중심으로 구성하였다.
> 한국사와 세계사의 통합 영역으로는 '역사 학습의 기초', '문명의 발생과
> 고대 세계의 형성'이 있다. 세계사 관련 영역으로는 '세계 종교의 확산과
> 지역 문화의 발전', '지역 세계의 교류와 변화', '제국주의와 국민 국가
> 건설 운동', '세계 대전과 사회 변동', '현대 세계의 전개와 과제'가 있다.
> 한국사 관련 영역은 세계사와의 연계를 고려하였으며, '국가의 형성과
> 발전', '통일신라와 발해', '고려의 성립과 변천', '조선의 성립과 발전',
> '조선 사회의 변동', '근⋅현대 사회로의 전환'이 있다.
> (별책7, 물리 79쪽 / 인쇄쪽수 73)

이 문단의 영역 이름 나열 순서를 코드의 영역 번호(`9역NN`)에 대응하면
01=역사 학습의 기초, 02=문명의 발생과 고대 세계의 형성, 03=세계 종교의
확산과 지역 문화의 발전, 04=지역 세계의 교류와 변화, 05=제국주의와 국민
국가 건설 운동, 06=세계 대전과 사회 변동, 07=현대 세계의 전개와 과제,
08=국가의 형성과 발전, 09=통일신라와 발해, 10=고려의 성립과 변천, 11=조선의
성립과 발전, 12=조선 사회의 변동, 13=근⋅현대 사회로의 전환이 된다.

이 중 **03~07("세계사 관련 영역")과 08~13("한국사 관련 영역")은 문서로
뒷받침된다** — 위 문단이 직접 그렇게 이름 붙였고, `한국사.json`(분리
전)에 실제로 기록되어 있던 `domain` 필드도 `9역06`="세계 대전과 사회
변동", `9역08`="국가의 형성과 발전", `9역09`="통일신라와 발해",
`9역10`="고려의 성립과 변천", `9역11`="조선의 성립과 발전",
`9역12`="조선 사회의 변동", `9역13`="근⋅현대 사회로의 전환"으로 위
문단과 글자 그대로 일치했다(01~05, 07은 추출 스크립트가 영역 표제를
못 찾아 domain이 비어 있거나 직전 값이 남아 있는 알려진 한계 — 위
"domain 필드" 절 참고. 코드 번호 자체는 이 한계와 무관하게 정확하다).

반면 **01~02("역사 학습의 기초", "문명의 발생과 고대 세계의 형성")는
문서상 세계사로 단정할 근거가 없다** — 원문이 이 둘을 "한국사와 세계사의
통합 영역"이라고 명시적으로 불렀기 때문이다. 이 저장소는 다음 두 가지
정황을 근거로 우선 세계사 쪽에 임시로 배정해 두었다: (1) 중학교 「역사」
교과서에서 이 두 영역은 통상 역사①(세계사 쪽 절반)에 실린다, (2) 실제
성취기준 문장(`[9역02-01]` "선사 문화 및 문명의 형성을 이해하고, 각
문명의 특징을 비교한다.", `[9역02-02]` "서아시아⋅지중해 세계에서 등장한
여러 정치체를 비교하고 종교 및 문화를 탐구한다.", `[9역02-03]` "중국과
인도의 정치적 변화와 종교⋅사상 성립의 관계를 이해하고, 유라시아의 상호
교류를 탐색한다.")이 한국 특정 내용 없이 전부 세계 문명 이야기다. 이
배정은 문서로 확정된 사실이 아니라 판단이었으나, **2026-09-20 담당자(대표)가
"통합 영역 01~02는 세계사로 둔다"고 확정했다**(작업 원장 기록). 대상 5개
코드: `[9역01-01]`, `[9역01-02]`, `[9역02-01]`, `[9역02-02]`,
`[9역02-03]`(모두 원래 domain이 비어 있어, 나중에 다시 나눠야 할 경우
쉽게 찾을 수 있도록 `domain`을 `'통합'`으로 채워 두었다 — 텍스트는
그대로다).

반대쪽 경계인 `[9역08-01]` "고조선과 여러 나라의 형성 과정 및 사회
모습을 탐구한다."부터는 한국사 고유 내용으로 분명하게 바뀐다.

### 코드 범위 → 과목 규칙표

| 코드 범위 / 라벨 | 과목 | 근거 (별책7 쪽수 / 표제) |
| --- | --- | --- |
| `[9역01-XX]` ~ `[9역07-XX]` (중학교 「역사」, 영역 01~07) | 세계사 | 03~07은 물리 79쪽/인쇄 73쪽 "세계사 관련 영역: 세계 종교의 확산과 지역 문화의 발전(03)⋅지역 세계의 교류와 변화(04)⋅제국주의와 국민 국가 건설 운동(05)⋅세계 대전과 사회 변동(06)⋅현대 세계의 전개와 과제(07)"로 문서 확정. **01(역사 학습의 기초)⋅02(문명의 발생과 고대 세계의 형성)는 원문이 "한국사와 세계사의 통합 영역"이라 부른 부분** — 실제 성취기준이 전부 세계 문명 내용이라는 정황으로 세계사에 두었고, 2026-09-20 담당자가 세계사로 확정 |
| `[9역08-XX]` ~ `[9역13-XX]` (중학교 「역사」, 영역 08~13) | 한국사 | 물리 79쪽/인쇄 73쪽 "한국사 관련 영역: 국가의 형성과 발전(08)⋅통일신라와 발해(09)⋅고려의 성립과 변천(10)⋅조선의 성립과 발전(11)⋅조선 사회의 변동(12)⋅근⋅현대 사회로의 전환(13)" |
| `[10한사1-XX-XX]`, `[10한사2-XX-XX]` (고등학교 공통 과목 「한국사1, 2」) | 한국사 | 물리 78쪽/인쇄 72쪽 "공통 과목은 '한국사1, 2'이다... 중학교 '역사'의 한국사 학습에 이어 근현대 한국사를 중심으로 구성하였다." |
| `[12동역XX-XX]` (고등학교 진로선택 「동아시아 역사 기행」) | 세계사 | 물리 78~79쪽/인쇄 72~73쪽 "진로선택 과목인 '동아시아 역사 기행'"; "동북아시아와 동남아시아로 구성된 동아시아의 각 지역이..." — 한국을 단독으로 다루지 않는 지역사이며, 성취기준 문장(예 `[12동역01-01]` "동아시아의 범위와 특징을 파악한다")에도 한국 특정 내용이 없어 세계사로 분류(비고: 브리프가 지정한 규칙) |
| `[12역현XX-XX]` (고등학교 융합선택 「역사로 탐구하는 현대 세계」) | 세계사 | 물리 79쪽/인쇄 73쪽 "융합선택 과목인 '역사로 탐구하는 현대 세계'... 국가, 지역, 세계의 역사를 상호 연관적으로 구성"; 실제 영역명(현대 세계와 역사 탐구/냉전과 열전/성장의 풍요와 생태환경/도전받는 현대 세계)과 성취기준(냉전, 기후변화, 세계화 등)이 전지구적 현대사 주제이고 한국 특정 내용이 없어 세계사로 분류 |
| `[12세사XX-XX]` (고등학교 일반선택 「세계사」, 13건) | 세계사 | 코드 라벨 자체가 `세사`. 2026-09-20 최종 검토(I7)에서 `사회.json`에서 `세계사.json`으로 옮김(텍스트 그대로, `subject`만 변경). `classifyHistory()`도 일반 규칙으로 지원한다 |

### 결과 건수

| 파일 | 전체 | 중 | 고 |
| --- | --- | --- | --- |
| `한국사.json` | 46 | 20 (`9역08`~`13`) | 26 (`10한사1/2`) |
| `세계사.json` | 59 | 20 (`9역01`~`07`) | 39 (`12동역` 12 + `12역현` 14 + `12세사` 13) |

`code`/`text`/`level`/`grade_band`는 분리 전 `한국사.json`과 바이트
단위로 동일하다. `domain`도 87건은 동일하고, 위 5개 코드(`9역01-01/02`,
`9역02-01/02/03`, "통합 영역")만 원래 비어 있던 값을 `'통합'`으로
채웠다(텍스트는 그대로). `subject`는 세계사로 옮긴 46건만 갱신되었다.
여기에 `사회.json`에서 옮겨 온 `[12세사]` 13건(텍스트·domain 그대로,
`subject`만 `세계사`)이 더해져 `세계사.json`은 59건이다. 한국사 46 +
세계사 59 = 105건·코드 중복 없음을 `tests/history-reclass.test.ts`와
`tests/standards-data.test.ts`(파일 목록에 `세계사.json` 추가)·
`tests/standards-continuity.test.ts`(파일별 prefix 연속성, 역사 영역이
파일 경계에서 쪼개지지 않으므로 그대로 통과)로 확인한다.

### 스크립트 사용법 (`scripts/reclassify-history.ts`)

CLI는 두 모드로 나뉜다:

- **split 모드** — `--source <92행 원본 json 경로>`를 줄 때만 실행된다.
  분리 전(모두 `subject:'한국사'`) 92행 원본을 `classifyHistory()`로
  다시 나눠 `data/standards/한국사.json`/`세계사.json`을 덮어쓴다. 지금
  저장소의 `한국사.json`은 이미 46행으로 분리되어 있어 이 모드의 기본
  입력으로 쓸 수 없으므로, `--source` 없이는 이 모드가 아예 실행되지
  않는다(재분리가 필요할 때만 분리 전 원본 파일 경로를 명시해서 쓴다).
- **update-db 모드(기본, `--source` 없을 때)** — `data/standards/세계사.json`을
  읽어 모든 행이 `subject:'세계사'`인지 검증(`codesToUpdate`)한 뒤, 그
  code 목록으로 호스팅 DB `standards` 테이블을
  `update standards set subject='세계사' where code in (...)`로(supabase-js
  `.update({subject:'세계사'}).in('code', codes)`, 100개씩 배치) 갱신하고
  행 수만 출력한다. 파일은 건드리지 않는다. `세계사.json`에는 `[12세사]`
  13건도 들어 있으므로, 다음 DB 실행 때 그 13건도 함께 `세계사`로
  바뀐다(별도 스크립트 불필요, 59행 갱신 예상).

```bash
# 마이그레이션(세계사 관련 변경) 적용 후, DB만 갱신
node --env-file=.env.local node_modules/tsx/dist/cli.mjs scripts/reclassify-history.ts

# (필요할 때만) 분리 전 92행 원본으로 파일을 다시 나눔
node --env-file=.env.local node_modules/tsx/dist/cli.mjs scripts/reclassify-history.ts \
  --source /path/to/한국사-92-original.json
```

update-db 모드에서 env(`NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`)가
없으면 DB를 건드리지 않고 `missing env: ...` 에러를 출력한 뒤 종료 코드
1로 실패한다(파일 분리와 달리 조용히 건너뛰지 않는다).

## 원문 대조 (2026-09-26)

DB의 모든 성취기준이 `standards.verified_at`/`verified_by` 없이 "미검증"으로
남아 있었다 — 별책 PDF에서 정규식으로 뽑아낸 뒤(위 "추출 방법"), 사람이 한
줄씩 원문과 대조한 적이 없었기 때문이다. 별책 PDF와 별개로, 평가원이 배포한
성취수준 PDF(`(초등학교/중학교)2022 개정 교육과정에 따른 성취수준(과목)`)를
`scripts/parse_levels.py`가 독립적으로 파싱해 `data/reference/levels/*.json`에
저장해 두었고(`lib/reference/levels.ts`가 읽는다), 각 레코드가 성취기준
`code`와 그 원문 `text`를 그대로 담고 있다. 별책 추출과 성취수준 파싱은
서로 다른 원본 PDF·다른 파서를 쓰므로, 같은 코드의 문장이 두 자료에서
일치하면 별책 추출이 오타 없이 옳았다고 볼 수 있는 독립 교차검증이 된다.

**방법** — `lib/standards/crosscheck.ts`의 `crosscheckStandards(dbRows, levelRecords)`:
같은 `code`의 두 문장을 `normalizeStandardText()`(가운뎃점 계열 `·`/`ㆍ`/`⋅`
통일, 인용부호 통일, 후행 마침표 `.`/`。` 제거, 그 뒤 공백을 한 칸으로 접는
게 아니라 전부 제거)로 정규화한 뒤 글자 단위로 비교한다. 코드가 같고
정규화한 문장도 같으면 `verified`, 코드는 같은데 문장이 다르면
`mismatched`(사람이 판단), 그 코드 자체가 `data/reference/levels/*.json`
어디에도 없으면 `unmatched`(대조 자료가 아예 없음 — 틀렸다는 뜻이 아니다).
성취수준 PDF·`levelFileFor()`가 아직 커버하지 못하는 범위(고등학교 전
과목, 한국사·세계사 중 성취수준 PDF가 없는 부분)는 항상 `unmatched`로
남는다.

공백을 한 칸으로 접는 대신 아예 지우는 것은 최초 구현(2026-09-26 1차) 리뷰
결과다 — 그 버전으로 나온 불일치 82건을 전부 읽어 보니 76건이
"비교 할"↔"비교할"류, PDF 줄바꿈 지점에 생긴 순수 공백 아티팩트였다(방향은
둘 다 있었다). 한국어는 "몇 칸 띄었나"가 아니라 "공백이 있냐 없냐" 자체가
줄바꿈 우연에 좌우되므로, 한 칸으로 접는 정도로는 이 차이를 못 잡는다.
공백을 전부 없애도 실제 표기 차이는 여전히 걸러진다 — "사회⋅문화적"(→
"사회·문화적")과 "사회 문화적"(→ "사회문화적")은 가운뎃점 유무로 여전히
다르고, "세계의 여러 지역"(→ "세계의여러지역")과 "세계 여러 지역"(→
"세계여러지역")도 조사 "의" 유무로 여전히 다르다(`tests/standards-crosscheck.test.ts`에
회귀 테스트로 고정).

**오프라인 보고서** — `npx tsx scripts/crosscheck_standards.ts`가
`data/standards/*.json` 전체(1,814행)를 `data/reference/levels/*.json`과
대조해 `data/standards/crosscheck-2026-09-26.md`(과목별 검증됨/불일치/대조
불가 집계 + 전체 불일치 표)를 쓰고 총계를 콘솔에 출력한다. DB는 건드리지
않는다(파일 대 파일 대조). 2026-09-26 실행 결과(공백 전부 제거 적용 후):
검증됨 724 / 불일치 6 / 대조 불가 1,084 (합계 1,814) — 남은 불일치 6건은
전부 실제 표기 차이다: 조사 유무(`[6사10-01]` "세계의"/"세계", `[6사12-01]`
"세계의"/"세계", `[9사(일사)01-03]` "사회에"/"사회에서") 3건, 가운뎃점 유무
(`[9국06-06]` "사회⋅문화적"/"사회 문화적") 1건, 낱말 차이(`[6사11-01]`
"사회적 책임"/"책임", `[9사(일사)11-03]` "이러한"/"해당") 2건 — 표와 근거는
보고서 파일 참고.

**관리자 화면 반영** — `/admin/standards`의 "원문 자동 대조" 버튼
(`app/admin/standards/actions.ts`의 `runStandardsCrosscheck` 서버 액션,
관리자만)이 같은 매처를 DB 전체 행에 대해 실행해, `verified` 판정을 받은
행만 `verified_at = now()`/`verified_by = 현재 관리자`로 일괄 갱신한다
(500개씩 나눠 `update ... where id in (...)`). `mismatched`는 DB를 건드리지
않고 코드·DB 원문·성취수준 원문을 화면에 표로 보여줘 관리자가 직접 확인해
`verifyStandard`(기존 행별 버튼)로 처리하게 한다. `unmatched`는 미검증
상태 그대로 두고, 화면에 "성취수준 원문 자료가 없는 항목은 자동 대조
대상이 아니다"라는 안내만 띄운다.
